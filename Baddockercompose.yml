version: '3'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    container_name: my_web
    depends_on:
      - db

  db:
    image: postgres:latest
    environment:
      POSTGRES_PASSWORD: password
